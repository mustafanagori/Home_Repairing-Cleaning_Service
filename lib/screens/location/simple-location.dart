import 'package:flutter/material.dart';

class SimpleLocation extends StatelessWidget {
  const SimpleLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Simple Location! "),
        centerTitle: true,
      ),
    );
  }
}
